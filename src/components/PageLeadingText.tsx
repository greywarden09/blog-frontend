import React from 'react';

import { Box, Typography } from '@mui/material';

export default function PageLeadingText() {
    return (
        <Box sx={{ textAlign: 'center', m: 5 }}>
          <Typography variant='body1'>Blog application</Typography>
        </Box>
    );
}
